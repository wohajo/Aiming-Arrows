package view;

import components.abstracts.Element;
import javafx.scene.layout.GridPane;

public class GameGrid extends GridPane {

    public GameGrid(Element[][] Elements) {

    }

    private void setupGameGrid(Element[][] Elements) {
        // for (int i = 0; i < E)
    }
}
